<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FabricaSapatos"
        tools:targetApi="31">
        <activity
            android:name=".Produto.TelaInsereProdutos"
            android:exported="false"
            android:label="@string/title_activity_tela_insere_produtos"
            android:theme="@style/Theme.FabricaSapatos" />
        <activity
            android:name=".Produto.TelaPedidos"
            android:exported="false"
            android:label="@string/title_activity_tela_pedidos"
            android:theme="@style/Theme.FabricaSapatos" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FabricaSapatos">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TelaClientes" />
        <activity android:name=".TelaInsereClientes" />
        <activity android:name=".TelaMostrarClientes" />
        <activity android:name=".TelaEditarClientes" />
    </application>

</manifest>